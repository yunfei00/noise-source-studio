# 已知问题

## 偶发退出（当前暂未复现）

- 多模型切换曾出现偶发退出。
- CPU/CUDA 推理曾出现偶发退出。
- 问题当前无法稳定复现，不能据此认定问题已经彻底解决。
- 2026-08-01 本阶段 GUI 验收曾捕获一次 Windows 原生异常 `0x8001010d`，随后的同流程
  复验正常退出且退出代码为 `0`；该问题仍无法稳定复现，也没有归因到具体模块。
- 如果问题再次发生，必须保留应用日志、`noise-source-studio-fault.log`、
  `application-exit.status` 和进程退出代码，并记录模型、计算设备及复现步骤。
- 当前阶段暂不进行推理进程架构重构，不引入 QProcess、multiprocessing、IPC 或模型双进程切换。
