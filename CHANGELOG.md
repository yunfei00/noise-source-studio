# Changelog

本项目遵循语义化版本。

## [Unreleased]

## [0.2.0] - 2026-09-22

### Phase 5A

- 增加统一 `TaskHistoryRecord`、三类任务枚举和七种跨模块生命周期状态。
- 增加用户应用数据目录中的 SQLite v1 历史索引、短连接 repository、WAL、迁移和备份。
- 单文件成功后自动保存轻量任务目录；失败任务只记录错误，不生成虚假预测结果。
- 批量与验证任务复用已有导出文件登记历史，不改变推理、指标或结果内容。
- 任务历史页增加概览、数据库分页、排序、完整筛选、状态图标、详情、备注标签和 CSV 导出。
- 增加三类历史结果原页面恢复、完整性校验、输出目录扫描、稳定旧任务 ID 和原子索引重建。
- 增加输出根目录边界检查、索引与文件分离删除，以及历史模块失败时的应用降级。
- 增加 10,000 条历史任务性能基准及数据库、服务、GUI 和恢复自动化测试。

### Batch UI performance

- 文件任务表改为 `QTableView`、`QAbstractTableModel` 与筛选代理，使用 item id 索引增量更新。
- 增加 150 ms item 更新合并、350 ms 摘要刷新和统计页 2 秒可见节流。
- 运行期间不再逐文件重建表格、重扫筛选选项、全量统计或自适应列宽。
- 批量 worker 使用缓存计数增量记录单文件状态转换，终态执行一次全量校准。
- 增加 100 / 1000 / 10000 行性能测试、独立基准脚本和 UI 刷新计数器。
- 增加主线程、后台线程、原生故障日志与正常退出标志；偶发退出仍列为待复现问题。

### Compute device

- 增加 `device_preference` 与 `allow_cpu_fallback` 用户配置及旧字段迁移。
- 增加后台 CPU/CUDA 设备探测、逐设备 tensor 计算与 synchronize 健康检查。
- 自动模式只选择完整探测成功的 CUDA，否则非阻塞回退 CPU。
- 明确 CUDA 加载失败时提供 CPU 回退、取消和查看日志选择，不静默修改配置。
- 顶部和系统设置区分设备策略与 session 实际设备。
- 模型切换改为候选 session 成功后原子替换，失败时保留旧 session。
- 单文件、批量、验证和模型加载期间统一锁定设备切换。

### Phase 4

- 增加 manifest 驱动的模型验证中心和六个验证页签。
- 增加相对/绝对路径、UTF-8 BOM、动态标签列、冲突标签、重复与缺失文件检查。
- 复用单一 `InferenceSession` 和批量任务的协作式暂停、继续、停止能力。
- 集中实现总体、逐标签、组合、Top-K、混淆矩阵和动态元数据分组指标。
- 增加万级虚拟样本表、错误样本筛选、统计图与矩阵点击联动及按需波形。
- 增加完整验证结果目录、历史恢复、自包含 HTML 报告和已有批量结果转验证。
- 推理失败样本单独计数，不进入模型准确率指标分母。

### Added

- 增加批量任务与文件任务的显式状态模型。
- 增加多文件、文件夹递归扫描、自然排序、路径去重和拖拽队列。
- 增加单工作线程顺序批量推理，复用当前已加载的 `InferenceSession`。
- 增加协作式暂停、继续和停止，以及失败项重试。
- 增加 item-level 错误隔离和 batch-level fatal error 分类。
- 增加批量进度、统计、动态详情、搜索和状态筛选。
- 增加 `summary.json`、UTF-8 BOM `predictions.csv` 与 `errors.csv` 自动导出。
- 增加 `task.json` 完整任务快照和无需重新推理的历史结果恢复。
- 增加文件任务、预测结果、统计分析三个页签。
- 增加万级虚拟结果表格、组合/来源/可信度筛选、低置信度规则和筛选结果导出。
- 增加动态概率详情、按需 CSV 波形、统计图筛选联动和 2～5 项结果对比。
- 增加批量 fake runtime 测试和环境变量驱动的三文件真实一致性测试。

### Changed

- 单文件页和批量 worker 共用同一 runtime 结果归一化函数。
- README 增加批量运行、停止语义、导出目录和真实集成测试说明。

### Notes

- 批量推理保持单 session、单工作线程顺序执行，不包含并行 forward。
- 尚未实现 SQLite 历史、多 GPU、远程推理或安装包。

## [0.1.0] - 2026-07-29

### Added

- 初始化 PySide6 商业 GUI 架构。
- 完成工作台、单文件预测、批量预测、模型验证、模型管理、任务历史、系统日志和系统设置八个主页面。
- 完成配置、路径、日志和异常处理基础设施。
- 完成统一导航、工作台快捷入口和商业界面收尾。
- 增加推理接口占位实现；0.1.0 尚未接入真实模型。
